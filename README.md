# Markowitz-Portfolio-Risk-Analyzer
A Python script for downloading historical stock data from Yahoo Finance, validating input parameters, and calculating key portfolio statistics. The program loads configuration from config.json, fetches price data, saves prices and daily returns to CSV files, and computes expected portfolio return and risk based on user-defined weights.
